import { api, idempotencyKey, listFrom } from "../../../core/api.js";
import { escapeHtml } from "../../../core/utils.js";
import { productVariantsMarkup, readProductVariants, setupProductVariants } from "../../../core/product-variants-form.js";

const money = value => Number(value || 0).toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
const today = () => new Date().toISOString().slice(0, 10);

export function initPurchaseInvoice() {
  const root = document.querySelector(".purchase-page"), $ = selector => root.querySelector(selector), controller = new AbortController();
  const listen = (node, event, callback) => node?.addEventListener(event, callback, { signal: controller.signal });
  const invoice = { id: null, version: null, items: [] };
  let suppliers = [], categories = [], variants = [], disposed = false, productSearchTimer = null, productSearchSequence = 0;
  const feedback = (message, error = false) => { const node = $("#purchaseFeedback"); node.className = `purchase-feedback${error ? " is-error" : ""}`; node.textContent = message; };
  const totals = () => { const subtotal = invoice.items.reduce((sum, item) => sum + item.quantity * item.cost, 0), lineDiscount = invoice.items.reduce((sum, item) => sum + item.discount, 0), discount = Number($("#piDiscount").value || 0), shipping = Number($("#piShipping").value || 0), total = Math.max(0, subtotal - lineDiscount - discount + shipping), state = root.querySelector('[name="paymentState"]:checked')?.value || "full", paid = state === "full" ? total : state === "deferred" ? 0 : Number($("#piPaid").value || 0); return { subtotal, lineDiscount, discount, shipping, total, paid, remaining: Math.max(0, total - paid), state }; };
  const renderTotals = () => { const value = totals(); for (const [id, key] of Object.entries({ piSubtotal: "subtotal", piLineDiscount: "lineDiscount", piTotal: "total", piPaidTotal: "paid", piRemaining: "remaining" })) $(`#${id}`).textContent = money(value[key]); $("#piPaidField").hidden = value.state !== "partial"; $("#piDueField").hidden = value.state === "full"; $("#piMethodField").hidden = value.state === "deferred"; };
  const renderItems = () => { $("#piItems").innerHTML = invoice.items.map(item => `<tr data-id="${escapeHtml(item.id)}"><td><strong>${escapeHtml(item.name)}</strong><small>${escapeHtml(item.color)} · ${escapeHtml(item.size)}</small></td><td><span>${escapeHtml(item.barcode || "—")}</span><small>${escapeHtml(item.sku || "—")}</small></td><td><input class="input num" type="number" min="1" value="${item.quantity}" data-field="quantity"></td><td><input class="input num" type="number" min="0" step="0.01" value="${item.cost}" data-field="cost"></td><td><input class="input num" type="number" min="0" step="0.01" value="${item.discount}" data-field="discount"></td><td>${money(item.quantity * item.cost - item.discount)}</td><td><button class="btn-icon" type="button" data-remove>×</button></td></tr>`).join(""); $("#piEmpty").hidden = invoice.items.length > 0; $("#piCount").textContent = `${invoice.items.length} أصناف`; renderTotals(); };
  const add = variant => { const existing = invoice.items.find(item => item.id === variant.id); if (existing) existing.quantity++; else invoice.items.push({ ...variant, quantity: 1, discount: 0 }); renderItems(); };
  const normalizeVariants = products => products.flatMap(product => (product.product_variants || product.variants || []).map(variant => ({ id: String(variant.id), version: Number(variant.version || 1), name: product.name_ar || product.name, size: variant.size || "افتراضي", color: variant.color || "افتراضي", sku: variant.sku || "", barcode: variant.barcode || "", cost: Number(product.purchase_price || variant.purchase_price || 0) })));
  const normalizeSearchVariants = response => listFrom(response).flatMap(item => { const nested = normalizeVariants([item]); if (nested.length) return nested; const variant = item.variant || item.product_variant || item, product = item.product || item.products || item; const id = variant.variant_id || variant.id; return id ? [{ id: String(id), version: Number(variant.version || item.version || 1), name: product.name_ar || product.name || item.product_name || item.name_ar || item.name || "منتج", size: variant.size || "افتراضي", color: variant.color || "افتراضي", sku: variant.sku || product.sku || "", barcode: variant.barcode || product.barcode || "", cost: Number(item.purchase_price ?? product.purchase_price ?? variant.purchase_price ?? 0) }] : []; });
  let teardownProductVariants = () => {};
  const closeProductOverlay = () => { teardownProductVariants(); teardownProductVariants = () => {}; $("#piModalOverlay").hidden = true; $("#piModalBody").innerHTML = ""; };
  const openSupplierOverlay = () => {
    $("#piModalTitle").textContent = "إضافة مورد جديد";
    $("#piModalBody").innerHTML = `<form id="piSupplierForm" novalidate><div class="products-dialog__body"><div class="products-form-grid"><div class="field"><label for="piSupplierName">اسم المورد <b>*</b></label><input class="input" id="piSupplierName" name="name" maxlength="200" required></div><div class="field"><label for="piSupplierPhone">رقم الهاتف <b>*</b></label><input class="input" id="piSupplierPhone" name="phone" type="tel" dir="ltr" maxlength="50" required></div><div class="field"><label for="piSupplierAddress">العنوان</label><input class="input" id="piSupplierAddress" name="address"></div><div class="field"><label for="piSupplierStatus">الحالة</label><select class="select" id="piSupplierStatus" name="status"><option value="active">نشط</option><option value="inactive">غير نشط</option></select></div><div class="field products-form-grid__full"><label for="piSupplierNotes">ملاحظات</label><textarea class="input" id="piSupplierNotes" name="notes" rows="3"></textarea></div></div><p class="purchase-error" id="piSupplierError"></p></div><footer class="modal__actions"><button class="btn btn-outline" type="button" data-close-supplier>إلغاء</button><button class="btn btn-primary" type="submit">حفظ المورد واختياره</button></footer></form>`;
    $("#piModalOverlay").hidden = false;
    const supplierForm = $("#piSupplierForm");
    supplierForm.querySelector("[data-close-supplier]").addEventListener("click", closeProductOverlay);
    supplierForm.addEventListener("submit", async event => {
      event.preventDefault();
      const submitButton = supplierForm.querySelector('[type="submit"]');
      const name = $("#piSupplierName").value.trim(), phone = $("#piSupplierPhone").value.trim();
      if (!name || !phone) { $("#piSupplierError").textContent = "اسم المورد ورقم الهاتف مطلوبان."; return; }
      submitButton.disabled = true; $("#piSupplierError").textContent = "";
      try {
        const response = await api.post("/api/v1/admin/suppliers", { name, phone, address: $("#piSupplierAddress").value.trim() || null, notes: $("#piSupplierNotes").value.trim() || null, status: $("#piSupplierStatus").value || "active" });
        const supplier = response?.supplier || response?.data || response;
        if (!supplier?.id) throw new Error("تم حفظ المورد لكن لم يرجع الخادم بياناته.");
        suppliers.push(supplier);
        const option = new Option(supplier.name || name, supplier.id, true, true); $("#piSupplier").add(option);
        closeProductOverlay(); feedback(`تمت إضافة المورد «${supplier.name || name}» واختياره.`);
      } catch (error) { $("#piSupplierError").textContent = error.message; }
      finally { submitButton.disabled = false; }
    });
    requestAnimationFrame(() => $("#piSupplierName").focus());
  };
  const openProductOverlay = () => {
    $("#piModalTitle").textContent = "إضافة منتج جديد";
    $("#piModalBody").innerHTML = `<form id="piQuickProductForm" class="purchase-product-form" novalidate><div class="products-dialog__body"><fieldset class="products-form-section"><legend><span>1</span> المعلومات الأساسية</legend><div class="products-form-grid"><div class="field"><label for="piProductNameAr">اسم المنتج (عربي) <b>*</b></label><input class="input" id="piProductNameAr" name="name_ar" type="text" placeholder="مثال: ثوب غيث الملكي" required></div><div class="field"><label for="piProductNameEn">اسم المنتج (إنجليزي)</label><input class="input" id="piProductNameEn" name="name_internal" type="text" dir="ltr" placeholder="e.g., Ghaith Royal Thobe"></div><div class="field"><label for="piProductCategory">الفئة <b>*</b></label><div class="purchase-category-select"><select class="select" id="piProductCategory" name="category_id" required><option value="">اختر الفئة...</option>${categories.map(item => `<option value="${escapeHtml(item.id)}">${escapeHtml(item.name)}</option>`).join("")}</select><button class="btn btn-outline" type="button" data-new-category>فئة جديدة</button></div></div><div class="field"><label for="piProductSku">الباركود (اختياري)</label><div class="products-sku-field"><input class="input" id="piProductSku" name="barcode" type="text" dir="ltr" placeholder="ينشئه الخادم تلقائيًا"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 5v14M7 5v14M11 5v14M15 5v14M21 5v14M18 5v14"/></svg></div></div><div class="field"><label for="piProductStatus">حالة المنتج</label><select class="select" id="piProductStatus" name="status"><option value="active">نشط</option><option value="inactive">غير نشط</option></select></div></div><section class="purchase-category-form" data-category-form hidden><h3>إضافة فئة جديدة</h3><div class="field"><label for="piNewCategoryName">اسم الفئة</label><input class="input" id="piNewCategoryName" type="text" placeholder="أدخل اسم الفئة" required></div><div class="field"><label for="piNewCategoryDescription">الوصف</label><textarea class="input" id="piNewCategoryDescription" rows="4" placeholder="أدخل وصفاً تفصيلياً للفئة..."></textarea></div><div class="field"><label for="piNewCategoryStatus">الحالة</label><select class="select" id="piNewCategoryStatus"><option value="active">نشط</option><option value="inactive">غير نشط</option></select></div><p class="purchase-error" data-category-error></p><div class="purchase-category-form__actions"><button class="btn btn-outline" type="button" data-cancel-category>إلغاء</button><button class="btn btn-primary" type="button" data-save-category>حفظ الفئة</button></div></section></fieldset><fieldset class="products-form-section"><legend><span>2</span> التسعير والمخزون</legend><div class="products-form-grid"><div class="field"><label for="piProductSalePrice">سعر البيع <b>*</b></label><div class="products-money-field"><input class="input num" id="piProductSalePrice" name="sale_price" type="number" min="0.01" step="0.01" placeholder="0.00" required><span>EGP</span></div></div><div class="field"><label for="piProductCostPrice">سعر التكلفة <b>*</b></label><div class="products-money-field"><input class="input num" id="piProductCostPrice" name="purchase_price" type="number" min="0" step="0.01" placeholder="0.00" required><span>EGP</span></div></div><div class="field"><label for="piProductSalesPercentage">نسبة المبيع (السيلز)</label><div class="products-percent-field"><input class="input num" id="piProductSalesPercentage" name="commission_rate" type="number" min="0" max="100" step="0.01" value="0"><span>%</span></div></div><div class="field"><label for="piProductNetProfitPercentage">صافي الربح</label><div class="products-percent-field"><input class="input num products-calculated-field" id="piProductNetProfitPercentage" type="text" value="0.00" readonly><span>%</span></div><small>يُحسب تلقائياً بعد خصم التكلفة ونسبة السيلز</small></div><div class="field"><label for="piProductQuantity">كمية فاتورة المشتريات</label><input class="input num" id="piProductQuantity" name="purchase_quantity" type="number" min="1" step="1" value="1" required></div><div class="field"><label for="piProductMinimum">الحد الأدنى للتنبيه <small>⚠</small></label><input class="input num" id="piProductMinimum" name="low_stock_threshold" type="number" min="0" value="5"></div></div>${productVariantsMarkup("piProductVariant")}</fieldset><p class="purchase-error" id="piQuickProductError"></p></div><footer class="modal__actions products-dialog__actions"><button class="btn btn-outline" type="button" data-close-product>إلغاء</button><button class="btn btn-primary" type="submit">حفظ المنتج وإضافته للفاتورة</button></footer></form>`;
    $("#piProductSku")?.closest(".field")?.remove();
    $("#piModalOverlay").hidden = false;
    const quickForm = $("#piQuickProductForm");
    const quantityInput = quickForm.elements.purchase_quantity;
    teardownProductVariants = setupProductVariants(quickForm, quantityInput);
    const updateProfit = () => { const sale = Number(quickForm.elements.sale_price.value) || 0, cost = Number(quickForm.elements.purchase_price.value) || 0, commission = Number(quickForm.elements.commission_rate.value) || 0, profit = sale > 0 ? ((sale - cost - sale * commission / 100) / sale) * 100 : 0; const output = $("#piProductNetProfitPercentage"); output.value = profit.toFixed(2); output.classList.toggle("is-negative", profit < 0); };
    [quickForm.elements.sale_price, quickForm.elements.purchase_price, quickForm.elements.commission_rate].forEach(input => input.addEventListener("input", updateProfit));
    const categoryForm = quickForm.querySelector("[data-category-form]"), categoryName = $("#piNewCategoryName");
    quickForm.querySelector("[data-new-category]").addEventListener("click", () => { categoryForm.hidden = false; requestAnimationFrame(() => categoryName.focus()); });
    quickForm.querySelector("[data-cancel-category]").addEventListener("click", () => { categoryForm.hidden = true; categoryName.value = ""; $("#piNewCategoryDescription").value = ""; categoryForm.querySelector("[data-category-error]").textContent = ""; });
    quickForm.querySelector("[data-save-category]").addEventListener("click", async event => { const saveButton = event.currentTarget, name = categoryName.value.trim(), errorNode = categoryForm.querySelector("[data-category-error]"); if (!name) { errorNode.textContent = "يرجى إدخال اسم الفئة."; categoryName.focus(); return; } saveButton.disabled = true; errorNode.textContent = ""; try { const response = await api.post("/api/v1/admin/categories", { name, description: $("#piNewCategoryDescription").value.trim() || null, status: $("#piNewCategoryStatus").value }); const category = response?.category || response?.data || response; if (!category?.id) throw new Error("تم حفظ الفئة لكن لم يرجع الخادم بياناتها."); categories.push(category); const option = new Option(category.name || name, category.id, true, true); quickForm.elements.category_id.add(option); categoryForm.hidden = true; categoryName.value = ""; $("#piNewCategoryDescription").value = ""; } catch (error) { errorNode.textContent = error.message; } finally { saveButton.disabled = false; } });
    quickForm.addEventListener("submit", async event => { event.preventDefault(); const button = quickForm.querySelector('[type="submit"]'); button.disabled = true; try { await createProduct(quickForm); } catch (error) { $("#piQuickProductError").textContent = error.message; } finally { button.disabled = false; } });
    quickForm.querySelector("[data-close-product]").addEventListener("click", closeProductOverlay);
    requestAnimationFrame(() => quickForm.elements.name_ar.focus());
  };
  const createProduct = async form => {
    const values = Object.fromEntries(new FormData(form)), supplierId = $("#piSupplier").value, cost = Number(values.purchase_price), price = Number(values.sale_price), requestedVariants = readProductVariants(form, form.elements.purchase_quantity);
    if (!supplierId) throw new Error("اختر المورد أولًا قبل إضافة منتج جديد.");
    if (!values.name_ar.trim() || !values.category_id || cost < 0 || price <= 0 || !requestedVariants.some(item => item.quantity > 0)) throw new Error("راجع اسم المنتج والتصنيف والأسعار، وأدخل كمية لصنف واحد على الأقل.");
    const response = await api.post("/api/v1/admin/products", { name_ar: values.name_ar.trim(), name_internal: values.name_internal.trim() || null, category_id: values.category_id, supplier_id: supplierId, purchase_price: cost, sale_price: price, initial_stock: 0, low_stock_threshold: Math.max(0, Number(values.low_stock_threshold) || 0), commission_rate: Math.min(100, Math.max(0, Number(values.commission_rate) || 0)), variants: requestedVariants.map(item => ({ ...item, quantity: 0 })), status: values.status || "active" }, { headers: { "Idempotency-Key": idempotencyKey() } });
    let product = response?.product || response?.data || response;
    if (product.id && !(product.product_variants || product.variants)?.length) product = await api.get(`/api/v1/products/${encodeURIComponent(product.id)}`);
    const created = normalizeVariants([product]); if (!created.length) throw new Error("تم إنشاء المنتج لكن لم يرجع الباك نسخة المنتج.");
    const key = item => `${item.size.trim().toLocaleLowerCase("ar")}::${item.color.trim().toLocaleLowerCase("ar")}`;
    created.forEach((item, index) => { const variant = { ...item, cost }, requested = requestedVariants.find(entry => key(entry) === key(variant)) || requestedVariants[index]; variants.push(variant); if (requested?.quantity > 0) { add(variant); invoice.items.find(entry => entry.id === variant.id).quantity = requested.quantity; } });
    renderItems(); closeProductOverlay(); feedback(`تم إنشاء «${values.name_ar.trim()}» وإضافته للفاتورة.`);
  };
  const load = async () => { try { const [supplierResponse, categoryResponse] = await Promise.all([api.get("/api/v1/admin/suppliers", { query: { page: 1, page_size: 100, status: "active" } }), api.get("/api/v1/categories")]); if (disposed) return; suppliers = listFrom(supplierResponse); categories = listFrom(categoryResponse); $("#piSupplier").innerHTML = '<option value="">اختر المورد</option>' + suppliers.map(item => `<option value="${escapeHtml(item.id)}">${escapeHtml(item.name)}</option>`).join(""); const selected = JSON.parse(sessionStorage.getItem("ghaith-purchase-supplier") || "null"); if (selected?.id) { $("#piSupplier").value = String(selected.id); sessionStorage.removeItem("ghaith-purchase-supplier"); } feedback("تم تحميل الموردين والتصنيفات من الخادم."); } catch (error) { feedback(error.message, true); } };
  const payload = () => { const value = totals(); if (!$("#piSupplier").value || !invoice.items.length) throw new Error("اختر المورد وأضف صنفًا واحدًا على الأقل."); if (!$("#piDate").value) throw new Error("حدد تاريخ الفاتورة."); if (value.paid > value.total) throw new Error("المبلغ المدفوع لا يمكن أن يتجاوز الإجمالي."); return { supplier_id: $("#piSupplier").value, supplier_invoice_number: null, invoice_date: $("#piDate").value, due_date: $("#piDue").value || null, notes: $("#piNotes").value.trim() || null, discount_amount: value.discount, shipping_amount: value.shipping, payment_state: value.state, payment_method: $("#piMethod").value, paid_amount: value.paid, payment_reference: $("#piReference").value.trim() || null, items: invoice.items.map(item => ({ variant_id: item.id, quantity: item.quantity, unit_cost: item.cost, discount_amount: item.discount, expected_version: item.version })) }; };
  const purchaseResponse = response => { const value = response?.invoice || response?.data || response; if (typeof value !== "string") return value || {}; try { return JSON.parse(value); } catch { return { id: value }; } };
  const saveDraft = async () => { const body = payload(), key = idempotencyKey(); const response = invoice.id ? await api.patch(`/api/v1/admin/purchase-invoices/${encodeURIComponent(invoice.id)}/draft`, { ...body, expected_version: invoice.version }, { headers: { "Idempotency-Key": key } }) : await api.post("/api/v1/admin/purchase-invoices/drafts", body, { headers: { "Idempotency-Key": key } }); const saved = purchaseResponse(response); if (!saved.id) throw new Error("تم إرسال المسودة لكن لم يرجع الخادم معرّف الفاتورة."); invoice.id = saved.id; invoice.version = Number(saved.version || invoice.version || 1); if (saved.invoice_number) $("#piInvoiceNumber").textContent = saved.invoice_number; $("#purchaseStatus").textContent = "مسودة محفوظة"; $("#piSaveStatus").textContent = `آخر حفظ: ${new Date().toLocaleTimeString("ar-EG")}`; return saved; };
  listen($("#piSearch"), "input", event => { const query = event.target.value.trim(), results = $("#piResults"); clearTimeout(productSearchTimer); if (!query) { productSearchSequence += 1; results.hidden = true; results.innerHTML = ""; return; } results.hidden = false; results.innerHTML = "<p>جاري البحث...</p>"; const sequence = ++productSearchSequence; productSearchTimer = setTimeout(async () => { try { const response = await api.get("/api/v1/products/search", { query: { q: query, in_stock: false, page: 1, page_size: 20 } }); if (disposed || sequence !== productSearchSequence) return; const matches = normalizeSearchVariants(response); matches.forEach(item => { const index = variants.findIndex(entry => entry.id === item.id); if (index >= 0) variants[index] = item; else variants.push(item); }); results.innerHTML = matches.map(item => `<button type="button" data-add="${escapeHtml(item.id)}"><span>${escapeHtml(item.name)} <small>${escapeHtml(item.color)} · ${escapeHtml(item.size)}</small></span><b>${money(item.cost)} EGP +</b></button>`).join("") || "<p>لا توجد نتائج.</p>"; } catch (error) { if (sequence === productSearchSequence) results.innerHTML = `<p>${escapeHtml(error.message)}</p>`; } }, 300); });
  listen($("#piBarcode"), "keydown", async event => { if (event.key !== "Enter") return; event.preventDefault(); const code = event.target.value.trim(); let variant = variants.find(item => item.barcode === code || item.sku === code); if (!variant) { try { const response = await api.get(`/api/v1/products/barcode/${encodeURIComponent(code)}`); const item = response?.data || response; variant = { id: String(item.variant_id || item.id), version: Number(item.version || 1), name: item.name_ar || item.name, size: item.size || "افتراضي", color: item.color || "افتراضي", sku: item.sku || "", barcode: item.barcode || "", cost: Number(item.purchase_price || 0) }; variants.push(variant); } catch (error) { feedback(error.message, true); return; } } add(variant); event.target.value = ""; });
  listen(root, "click", async event => { const button = event.target.closest("button"); if (!button) return; try { if (button.dataset.add) add(variants.find(item => item.id === button.dataset.add)); if (button.hasAttribute("data-remove")) { invoice.items = invoice.items.filter(item => item.id !== button.closest("tr").dataset.id); renderItems(); } if (button.id === "piSaveDraft") { await saveDraft(); feedback("تم حفظ المسودة على الخادم."); } if (button.id === "piAddSupplier") openSupplierOverlay(); if (button.id === "piAddProduct") location.hash = "products"; } catch (error) { feedback(error.message, true); } });
  listen(root, "input", event => { const row = event.target.closest("tr[data-id]"); if (row && event.target.dataset.field) { const item = invoice.items.find(entry => entry.id === row.dataset.id); item[event.target.dataset.field] = Number(event.target.value || 0); renderTotals(); } else renderTotals(); });
  listen(root, "change", renderTotals);
  listen($("#purchaseEditor"), "submit", async event => {
    event.preventDefault();
    const submitButton = event.submitter || $("#purchaseEditor button[type='submit']");
    submitButton.disabled = true;
    try {
      await saveDraft();
      const response = await api.post(`/api/v1/admin/purchase-invoices/${encodeURIComponent(invoice.id)}/approve`, { expected_version: invoice.version }, { headers: { "Idempotency-Key": idempotencyKey() } });
      const approved = response?.invoice || response?.data || response;
      invoice.version = Number(approved.version || invoice.version);
      $("#purchaseStatus").textContent = "معتمدة";
      const message = `تم اعتماد فاتورة المشتريات ${approved.invoice_number || ""} وترحيل المخزون بنجاح.`;
      feedback(message);
      sessionStorage.setItem("ghaith-suppliers-success", message);
      await new Promise(resolve => setTimeout(resolve, 800));
      location.hash = "suppliers";
    } catch (error) {
      feedback(error.message, true);
      submitButton.disabled = false;
    }
  });
  $("#piDate").value = today(); $("#piHistory").innerHTML = '<p class="page-subtitle">الفواتير المعتمدة متاحة من شاشة المورد والمبيعات.</p>'; $("#purchaseResume").hidden = true; renderItems(); load();
  listen($("#piAddProduct"), "click", event => { event.stopPropagation(); openProductOverlay(); });
  listen($("#piModalOverlay"), "click", event => { if (event.target === $("#piModalOverlay") || event.target.id === "piCloseModal") closeProductOverlay(); });
  return () => { disposed = true; productSearchSequence += 1; clearTimeout(productSearchTimer); controller.abort(); };
}
